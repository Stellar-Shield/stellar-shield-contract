# Rust build output. This was tracked once: 1,522 files and 258 MB of compiler
# artifacts in a repository holding a few hundred lines of source.
/target
**/target

# Test snapshots are regenerated by cargo test and are safe to keep, but the
# editor and OS noise below is not.
.DS_Store
*.swp
.idea/
.vscode/
